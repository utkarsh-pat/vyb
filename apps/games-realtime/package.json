{
  "name": "@vyb/games-realtime",
  "version": "0.1.4",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "check": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "types": "wrangler types"
  },
  "dependencies": {
    "@vyb/game-engine": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.20.3",
    "typescript": "latest",
    "vitest": "^4.1.0",
    "wrangler": "^4.120.0"
  }
}
